create or replace PACKAGE PKG_ADMIN_PRODUCTOS AS 

  FUNCTION F_USUARIO_ACCESO_CUENTA (p_nombreusuario IN USUARIO.NOMBREUSUARIO%TYPE,
    p_cuenta_id IN CUENTA.ID%TYPE) RETURN NUMBER;

  FUNCTION F_OBTENER_PLAN_CUENTA(p_cuenta_id IN CUENTA.ID%TYPE) RETURN PLAN%ROWTYPE;

  FUNCTION F_CONTAR_PRODUCTOS_CUENTA(p_cuenta_id IN CUENTA.ID%TYPE) RETURN NUMBER;

  FUNCTION F_NUM_CATEGORIAS_CUENTA(p_cuenta_id IN CUENTA.ID%TYPE) RETURN NUMBER;

  FUNCTION F_VALIDAR_ATRIBUTOS_PRODUCTO(p_producto_gtin IN PRODUCTO.GTIN%TYPE,
  p_cuenta_id IN PRODUCTO.CUENTA_ID%TYPE) RETURN BOOLEAN;

  PROCEDURE P_ACTUALIZAR_NOMBRE_PRODUCTO(p_producto_gtin IN PRODUCTO.GTIN%TYPE, p_cuenta_id IN PRODUCTO.CUENTA_ID%TYPE, p_nuevo_nombre IN PRODUCTO.NOMBRE%TYPE);

  PROCEDURE P_ACTUALIZAR_TODAS_CUENTAS;

  PROCEDURE P_ACTUALIZAR_PRODUCTOS(p_cuenta_id IN cuenta.id%TYPE);

  PROCEDURE P_CREAR_USUARIO(p_usuario IN USUARIO%ROWTYPE, p_rol IN VARCHAR2, p_password IN VARCHAR2);

  EXCEPTION_PLAN_NO_ASIGNADO EXCEPTION;

  EXCEPTION_SIN_ACCESO_CUENTA EXCEPTION;

  INVALID_DATA EXCEPTION;

  EXCEPTION_USUARIO_YA_EXISTENTE EXCEPTION;

  EXCEPTION_ROL_INEXISTENTE EXCEPTION;

END PKG_ADMIN_PRODUCTOS;
